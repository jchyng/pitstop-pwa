# TODO

지인 테스트 이후 우선순위를 보면서 처리. 지금 당장 할 필요는 없거나, 결정이 필요한 항목들.

---

## 콘텐츠

- [ ] **취급설명서 이미지 수집** — `public/manuals/{car_id}/` 에 실제 이미지 넣기. 가이드 페이지의 "증거 보기" 버튼이 지금은 빈 껍데기. 3종 기준으로 먼저 수집 (아반떼 MD, 그랜저 HG, 렉스턴 스포츠 칸).
- [ ] **`manual_spec` 필드 채우기** — 각 차량 JSON의 소모품 항목에 공식 취급설명서 페이지 발췌 텍스트 입력.
- [ ] **그랜저 HG 2.4 vs 3.0 엔진 구분** — 현재 단일 JSON으로 통합돼 있음. 엔진별 정비 주기 차이가 있으면 `grandeur-hg-3.0-gasoline.json` 분리 필요. 실제 취급설명서 확인 후 결정.

---

## 기능 개선

- [ ] **fetch 에러 처리** — `page.tsx`, `cost/page.tsx`, `log/page.tsx`, `guide/page.tsx`의 fetch 실패가 현재 무시됨 (`.catch(() => {})`). 토스트 혹은 인라인 에러 메시지 패턴 결정 후 일괄 적용. 패턴을 먼저 정하는 게 중요.
- [ ] **`behavior: 'both'` 긴급도 분기** — `urgency.ts`에서 inspect/replace 주기가 별도인 항목 처리 로직 없음. 현재 차량 데이터에 `both` 항목이 없어 실제 영향은 없지만, 차종 확장 전에 구현 필요.
- [ ] **소모품 이모지** — 카드에 아이템별 이모지 표시. 디자인 미확정. 지인 피드백에서 필요성 확인 후 결정.

---

## 코드 품질

- [ ] **`page.tsx` 커스텀 훅 분리** — 약 1,000줄. `useCarData()`, `useConsumableItems()` 등으로 분리하면 가독성 향상. 기능 변화 없는 리팩토링 — 지금 당장은 우선순위 낮음.
- [ ] **`storage.ts` 유닛 테스트** — CRUD 함수 테스트 없음. Phase 2 (Supabase 마이그레이션) 전에 추가하면 마이그레이션 안전망이 됨.

---

## Phase 2 (지인 테스트 이후)

- [ ] **카카오 로그인** (Supabase Custom OAuth) — Supabase 네이티브 지원 안 됨, 커스텀 설정 필요 (+30~60분 추가 작업)
- [ ] **localStorage → Supabase PostgreSQL 마이그레이션**
- [ ] **기기 간 정비 이력 동기화**

---

## Phase 3

- [ ] **웹 푸시 알림** (Supabase Edge Functions + VAPID) — 이게 없으면 사용자가 앱을 능동적으로 열어야 함. PWA 핵심 가치.
- [ ] **iOS 홈 화면 추가 안내 UI** — iOS는 "홈 화면에 추가" 후에만 웹 푸시 동작. 설치 유도 배너 필요.
